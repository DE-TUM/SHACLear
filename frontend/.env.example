# Copy this file to `frontend/.env` and adjust if needed.
# The file `frontend/.env` is git-ignored and must never be committed.

# Base URL of the FastAPI backend.
# Development default: the uvicorn server started by `scripts/start_backend.sh`.
VITE_API_BASE=http://localhost:8000
